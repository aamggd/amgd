 { "الصنف غير نشط" }\n        require(db.warehouseDao().allActive().any { it.id == warehouseId }) { "المخزن غير موجود أو غير نشط" }\n        require(reason.isNotBlank()) { "سبب ربط الرصيد القديم مطلوب" }\n\n        val sourceLotKey = InventoryMath.lotKey(sourceLotNo)\n        val sourceExpiryKey = InventoryMath.expiryKey(sourceExpiryDate)\n        val source = db.stockDao().lotBalances(warehouseId, itemId).firstOrNull {\n            InventoryMath.lotKey(it.lotNo) == sourceLotKey && InventoryMath.expiryKey(it.expiryDate) == sourceExpiryKey\n        } ?: error("الرصيد القديم المحدد لم يعد متاحاً")\n\n        val targetLot = if (item.lotTracked) destinationLotNo?.trim()?.ifBlank { null } else sourceLotNo?.trim()?.ifBlank { null }\n        val targetExpiry = if (item.expiryTracked) destinationExpiryDate else sourceExpiryDate\n        InventoryMath.validateLegacyTrackingAssignment(\n            quantityBase = quantityBase,\n            availableBase = source.quantityBase,\n            lotTracked = item.lotTracked,\n            expiryTracked = item.expiryTracked,\n            sourceLotNo = sourceLotNo,\n            sourceExpiryDate = sourceExpiryDate,\n            destinationLotNo = targetLot,\n            destinationExpiryDate = targetExpiry\n        )\n\n        val unitCost = if (abs(source.quantityBase) <= InventoryMath.EPS) 0.0 else source.inventoryValueBase / source.quantityBase\n        require(unitCost.isFinite() && unitCost >= 0.0) { "تعذر تحديد تكلفة الرصيد القديم" }\n        val now = System.currentTimeMillis()\n        val referenceId = now\n\n        db.stockDao().insertMovement(\n            StockMovementEntity(\n                movementDate = now, warehouseId = w