eDao().insertAudit(\n            AuditEventEntity(\n                userId = changedBy, action = "RECLASSIFY", entityType = "INVENTORY_LOT_TRACKING", entityId = referenceId.toString(),\n                oldValue = "warehouse=$warehouseId item=$itemId lot=${sourceLotNo.orEmpty()} expiry=${sourceExpiryDate ?: -1} qty=$quantityBase",\n                newValue = "warehouse=$warehouseId item=$itemId lot=${targetLot.orEmpty()} expiry=${targetExpiry ?: -1} qty=$quantityBase",\n                reason = reason.trim()\n            )\n        )\n        referenceId\n    }\n\n'''\n)\n\nscope = ROOT / 'PHASE14_5_23_SCOPE.md'\nscope.write_text('''# Phase 14.5.23 — Legacy Lot / Expiry Assignment\n\n- Allows existing positive stock balances with missing required Lot and/or expiry tracking to be reclassified to a real lot/expiry.\n- Reclassification posts equal-value OUT and IN stock movements inside one Room transaction.\n- Total warehouse quantity and inventory value are unchanged.\n- Partial assignment is supported so one legacy balance may be split across multiple real lots.\n- Required tracking fields are validated and the operation is audit logged.\n- Existing lot-control status is carried forward when the destination has no explicit control record.\n- No Room schema change; schema remains 23.\n''', encoding='utf-8')\nprint('Phase 14.5.23 core patch applied')\nfrom pathlib import Path\nROOT = Path('FushERP_Mobile_Phase5')\n\ndef repl(rel: str, old: str, new: str) -> None:\n    p = ROOT / rel\n    text = p.read_text(encoding='utf-8')\n    if old not in text:\n        raise SystemExit(f'Pattern not found in {rel}: {old[:100]!r}')\n    p.write_text(text.replace(old, new, 1), encoding='utf-8')\n\ndef in