arehouseId, itemId = itemId,\n                movementType = "LOT_RECLASSIFY_OUT", quantityBase = -quantityBase, unitCostBase = unitCost,\n                referenceType = "LOT_RECLASSIFICATION", referenceId = referenceId,\n                lotNo = sourceLotNo?.trim()?.ifBlank { null }, expiryDate = sourceExpiryDate\n            )\n        )\n        db.stockDao().insertMovement(\n            StockMovementEntity(\n                movementDate = now, warehouseId = warehouseId, itemId = itemId,\n                movementType = "LOT_RECLASSIFY_IN", quantityBase = quantityBase, unitCostBase = unitCost,\n                referenceType = "LOT_RECLASSIFICATION", referenceId = referenceId,\n                lotNo = targetLot, expiryDate = targetExpiry\n            )\n        )\n\n        val sourceControl = db.advancedInventoryDao().lotControl(warehouseId, itemId, sourceLotKey, sourceExpiryKey)\n        val targetLotKey = InventoryMath.lotKey(targetLot)\n        val targetExpiryKey = InventoryMath.expiryKey(targetExpiry)\n        val targetControl = db.advancedInventoryDao().lotControl(warehouseId, itemId, targetLotKey, targetExpiryKey)\n        if (targetControl == null && sourceControl != null) {\n            db.advancedInventoryDao().upsertLotControl(\n                sourceControl.copy(\n                    id = 0, lotNo = targetLot, expiryDate = targetExpiry, lotKey = targetLotKey, expiryKey = targetExpiryKey,\n                    reason = listOf(sourceControl.reason, "منقول من رصيد قديم غير مكتمل التتبع").filter { it.isNotBlank() }.joinToString(" • "),\n                    changedBy = changedBy, changedAt = now\n                )\n            )\n        }\n\n        db.governanc