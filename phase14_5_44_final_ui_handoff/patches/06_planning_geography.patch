diff --git a/app/src/main/java/com/fush/erp/ui/screens/GeographyScreens.kt b/app/src/main/java/com/fush/erp/ui/screens/GeographyScreens.kt
index c326637..ec4f01b 100644
--- a/app/src/main/java/com/fush/erp/ui/screens/GeographyScreens.kt
+++ b/app/src/main/java/com/fush/erp/ui/screens/GeographyScreens.kt
@@ -13,6 +13,7 @@ import com.fush.erp.data.AppContainer
 import com.fush.erp.data.entity.*
 import com.fush.erp.domain.GeographicQuoteResult
 import com.fush.erp.domain.SalesMath
+import com.fush.erp.ui.*
 import kotlinx.coroutines.launch
 import java.text.SimpleDateFormat
 import java.util.*
@@ -25,6 +26,11 @@ fun CurrencyGeographyScreen(container: AppContainer, user: UserEntity, modifier:
     val tabs = listOf("أسعار الصرف", "المحافظات والأسعار", "حاسبة التسعير", "الربحية")
     var selected by remember { mutableIntStateOf(0) }
     Column(modifier.fillMaxSize()) {
+        FushSectionHeader(
+            title = "العملات والجغرافيا",
+            subtitle = "أسعار الصرف التاريخية، سياسات المحافظات، قوائم الأسعار والتسعير والربحية الجغرافية.",
+            modifier = Modifier.padding(horizontal = 12.dp, vertical = 10.dp),
+        )
         ScrollableTabRow(selectedTabIndex = selected) {
             tabs.forEachIndexed { index, title ->
                 Tab(selected = selected == index, onClick = { selected = index }, text = { Text(title) })
@@ -49,12 +55,13 @@ private fun FxHistoryTab(container: AppContainer, user: UserEntity, modifier: Mo
 
     LazyColumn(modifier.fillMaxSize().padding(12.dp), verticalArrangement = Arrangement.spacedBy(8.dp)) {
         item {
-            Text("أسعار الصرف التاريخية", style = MaterialTheme.typography.headlineSmall)
-            Text("كل سعر يحتفظ بتاريخ سريانه. سعر الريال القديم إلى الجديد يُشتق من سعر الدولار في الطبعتين.", style = MaterialTheme.typography.bodySmall)
+            FushSectionHeader("أسعار الصرف التاريخية", "كل سعر يحتفظ بتاريخ سريانه، ويُشتق تحويل الريال القديم إلى الجديد من سعر الدولار في الطبعتين.")
             Button(onClick = { showAdd = true }) { Text("تسجيل سعر يومي") }
-            message?.let { Text(it, color = MaterialTheme.colorScheme.primary) }
+            message?.let { FushNotice(it, tone = if (it.startsWith("تعذر")) FushStatusTone.Danger else FushStatusTone.Success) }
         }
-        if (snapshots.isNotEmpty()) {
+        if (snapshots.isEmpty()) {
+            item { FushEmptyState("لا توجد لقطات صرف يومية", "سجّل أول سعر يومي للدولار بالطبعتين لتكوين سجل تاريخي قابل للتتبع.") }
+        } else {
             item { Text("لقطات الدولار اليومية", style = MaterialTheme.typography.titleMedium) }
             items(snapshots, key = { it.id }) { row ->
                 ElevatedCard(Modifier.fillMaxWidth()) {
@@ -68,6 +75,7 @@ private fun FxHistoryTab(container: AppContainer, user: UserEntity, modifier: Mo
             }
         }
         item { Text("سجل أسعار التحويل إلى العملة الأساسية", style = MaterialTheme.typography.titleMedium) }
+        if (rates.isEmpty()) item { FushInlineState("لا توجد أسعار تحويل تاريخية للعملات حتى الآن.") }
         items(rates) { row ->
             ListItem(
                 headlineContent = { Text(row.currencyCode) },
@@ -135,11 +143,11 @@ private fun ProvincePricingTab(container: AppContainer, modifier: Modifier) {
 
     LazyColumn(modifier.fillMaxSize().padding(12.dp), verticalArrangement = Arrangement.spacedBy(8.dp)) {
         item {
-            Text("سياسات المحافظات وقوائم الأسعار", style = MaterialTheme.typography.headlineSmall)
-            Text("تعز هي الأساس. عدن يضاف لها نقل افتراضي 10,000 ريال لكل كرتون. صنعاء تستخدم الطبعة القديمة بسعر تحويل يومي.", style = MaterialTheme.typography.bodySmall)
+            FushSectionHeader("سياسات المحافظات وقوائم الأسعار", "تعز هي الأساس، ويمكن لكل محافظة تحديد العملة والنقل الافتراضي ومتطلبات سعر الصرف والرسوم.")
             Button(enabled = product != null && policies.isNotEmpty(), onClick = { showPrice = true }) { Text("إضافة سعر محافظة") }
-            message?.let { Text(it, color = MaterialTheme.colorScheme.primary) }
+            message?.let { FushNotice(it, tone = if (it.startsWith("تعذر")) FushStatusTone.Danger else FushStatusTone.Success) }
         }
+        if (policies.isEmpty()) item { FushEmptyState("لا توجد سياسات محافظات", "أضف سياسات المحافظات قبل إنشاء قوائم أسعار جغرافية.") }
         items(policies, key = { it.code }) { p ->
             ElevatedCard(Modifier.fillMaxWidth()) {
                 Column(Modifier.padding(12.dp), verticalArrangement = Arrangement.spacedBy(3.dp)) {
@@ -185,7 +193,10 @@ private fun ProductPriceList(container: AppContainer, product: ItemEntity) {
     Column(verticalArrangement = Arrangement.spacedBy(4.dp)) {
         Text("قائمة أسعار Fush 60 مل", style = MaterialTheme.typography.titleMedium)
         Text("البيع يستخدم فقط القائمة الفعالة والسارية في تاريخ الفاتورة لنفس المحافظة والقناة والعملة.", style = MaterialTheme.typography.bodySmall)
-        message?.let { Text(it, color = MaterialTheme.colorScheme.primary) }
+        message?.let { FushNotice(it, tone = if (it.startsWith("تعذر")) FushStatusTone.Danger else FushStatusTone.Success) }
+        if (prices.isEmpty()) {
+            FushInlineState("لا توجد قوائم أسعار لهذا المنتج حتى الآن.")
+        }
         prices.take(40).forEach { row ->
             ListItem(
                 headlineContent = {
@@ -285,8 +296,7 @@ private fun GeographicQuoteTab(container: AppContainer, modifier: Modifier) {
     LaunchedEffect(policies) { if (policy == null) policy = policies.firstOrNull() }
     LazyColumn(modifier.fillMaxSize().padding(12.dp), verticalArrangement = Arrangement.spacedBy(8.dp)) {
         item {
-            Text("حاسبة التسعير الجغرافي", style = MaterialTheme.typography.headlineSmall)
-            Text("النقل والرسوم في الحاسبة هي إضافات سعرية. التكاليف الفعلية تُسجل في تبويب الربحية حتى لا تختلط الإيرادات بالتكلفة.", style = MaterialTheme.typography.bodySmall)
+            FushSectionHeader("حاسبة التسعير الجغرافي", "النقل والرسوم هنا إضافات سعرية؛ أما التكاليف الفعلية فتسجل في الربحية حتى لا تختلط الإيرادات بالتكلفة.")
             GeoSelect("المحافظة", policy, policies, { it.nameAr }) { policy = it; result = null }
             OutlinedTextField(cartons, { cartons = it }, label = { Text("عدد الكراتين") }, singleLine = true)
             OutlinedTextField(pricePerCartonNew, { pricePerCartonNew = it }, label = { Text("سعر المنتج للكرتون بالريال الجديد قبل النقل") }, singleLine = true)
@@ -308,7 +318,7 @@ private fun GeographicQuoteTab(container: AppContainer, modifier: Modifier) {
                     } catch (e: Exception) { result = null; message = e.message ?: "تعذر الحساب" }
                 }
             }) { Text("احسب السعر") }
-            message?.let { Text(it, color = MaterialTheme.colorScheme.error) }
+            message?.let { FushNotice(it, tone = FushStatusTone.Danger) }
         }
         result?.let { q ->
             item {
@@ -352,16 +362,17 @@ private fun GeographicProfitabilityTab(container: AppContainer, user: UserEntity
 
     LazyColumn(modifier.fillMaxSize().padding(12.dp), verticalArrangement = Arrangement.spacedBy(8.dp)) {
         item {
-            Text("الربحية الجغرافية", style = MaterialTheme.typography.headlineSmall)
+            FushSectionHeader("الربحية الجغرافية", "تحليل صافي المبيعات والتكلفة والعمولة والتكاليف الجغرافية الفعلية حسب المحافظة والفاتورة.")
             Row(horizontalArrangement = Arrangement.spacedBy(6.dp)) {
                 OutlinedTextField(from, { from = it }, label = { Text("من") }, modifier = Modifier.weight(1f), singleLine = true)
                 OutlinedTextField(to, { to = it }, label = { Text("إلى") }, modifier = Modifier.weight(1f), singleLine = true)
             }
             Button(onClick = { reload() }) { Text("تحديث التقرير") }
             Text("الربح = صافي المبيعات − تكلفة البضاعة − العمولة المستحقة − النقل/الرسوم الفعلية المسجلة.", style = MaterialTheme.typography.bodySmall)
-            message?.let { Text(it, color = MaterialTheme.colorScheme.primary) }
+            message?.let { FushNotice(it, tone = if (it.startsWith("تعذر")) FushStatusTone.Danger else FushStatusTone.Success) }
         }
         item { Text("حسب المحافظة", style = MaterialTheme.typography.titleMedium) }
+        if (provinces.isEmpty()) item { FushInlineState("لا توجد نتائج ربحية حسب المحافظة ضمن الفترة المحددة.") }
         items(provinces, key = { it.province }) { p ->
             ElevatedCard(Modifier.fillMaxWidth()) {
                 Column(Modifier.padding(12.dp), verticalArrangement = Arrangement.spacedBy(3.dp)) {
@@ -373,6 +384,7 @@ private fun GeographicProfitabilityTab(container: AppContainer, user: UserEntity
             }
         }
         item { Text("حسب الفاتورة والعميل", style = MaterialTheme.typography.titleMedium) }
+        if (invoices.isEmpty()) item { FushInlineState("لا توجد فواتير ربحية ضمن الفترة المحددة.") }
         items(invoices, key = { it.invoiceId }) { r ->
             ElevatedCard(Modifier.fillMaxWidth()) {
                 Column(Modifier.padding(12.dp), verticalArrangement = Arrangement.spacedBy(3.dp)) {
diff --git a/app/src/main/java/com/fush/erp/ui/screens/PlanningScreen.kt b/app/src/main/java/com/fush/erp/ui/screens/PlanningScreen.kt
index 8656bcb..c99b6c0 100644
--- a/app/src/main/java/com/fush/erp/ui/screens/PlanningScreen.kt
+++ b/app/src/main/java/com/fush/erp/ui/screens/PlanningScreen.kt
@@ -12,6 +12,7 @@ import androidx.compose.ui.Modifier
 import androidx.compose.ui.unit.dp
 import com.fush.erp.data.AppContainer
 import com.fush.erp.data.entity.*
+import com.fush.erp.ui.*
 import kotlinx.coroutines.flow.flowOf
 import kotlinx.coroutines.launch
 import java.util.Locale
@@ -194,10 +195,9 @@ fun PlanningScreen(
         verticalArrangement = Arrangement.spacedBy(12.dp)
     ) {
         item {
-            Text("التخطيط والموسمية", style = MaterialTheme.typography.headlineSmall)
-            Text(
-                "المرحلة 14.5: الموازنة والموسمية + تحويل خطط الطلب المعتمدة إلى خطة إنتاج واحتياجات مواد ومخزون أمان ونقاط إعادة طلب.",
-                style = MaterialTheme.typography.bodyMedium
+            FushSectionHeader(
+                title = "التخطيط والموسمية",
+                subtitle = "الموازنة والموسمية وتحويل خطط الطلب المعتمدة إلى خطة إنتاج واحتياجات مواد ومخزون أمان ونقاط إعادة طلب.",
             )
         }
 
@@ -241,10 +241,10 @@ fun PlanningScreen(
                             }
                         }
                     }
-                    message?.let { Text(it, color = MaterialTheme.colorScheme.primary) }
-                    forecastError?.let { Text(it, color = MaterialTheme.colorScheme.error) }
-                    seasonalError?.let { Text(it, color = MaterialTheme.colorScheme.error) }
-                    productionPlanningError?.let { Text(it, color = MaterialTheme.colorScheme.error) }
+                    message?.let { FushNotice(it, tone = FushStatusTone.Success) }
+                    forecastError?.let { FushNotice(it, tone = FushStatusTone.Danger) }
+                    seasonalError?.let { FushNotice(it, tone = FushStatusTone.Danger) }
+                    productionPlanningError?.let { FushNotice(it, tone = FushStatusTone.Danger) }
                 }
             }
         }
@@ -255,7 +255,7 @@ fun PlanningScreen(
                     Text("توقع الشهر القادم", style = MaterialTheme.typography.titleMedium)
                     val f = forecast
                     if (f == null) {
-                        Text("لا توجد نتيجة بعد.")
+                        FushInlineState("لا توجد نتيجة بعد. اختر المنتج والمحافظة وانتظر اكتمال حساب التوقع.")
                     } else {
                         Text("${planningMonthNames[f.forecastMonth - 1]} ${f.forecastYear}", style = MaterialTheme.typography.titleLarge)
                         Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.spacedBy(8.dp)) {
@@ -282,7 +282,7 @@ fun PlanningScreen(
                     )
                     val a = seasonalAnalysis
                     if (a == null) {
-                        Text("لا توجد نتيجة بعد.")
+                        FushInlineState("لا توجد نتيجة موسمية بعد لهذا الاختيار.")
                     } else {
                         val actualLift = com.fush.erp.domain.PlanningMath.relativeDifferencePct(a.summerActualAvgQtyBase, a.winterActualAvgQtyBase)
                         val forecastLift = com.fush.erp.domain.PlanningMath.relativeDifferencePct(a.summerForecastMonthlyQtyBase, a.winterForecastMonthlyQtyBase)
@@ -314,7 +314,7 @@ fun PlanningScreen(
                 Column(Modifier.padding(14.dp), verticalArrangement = Arrangement.spacedBy(10.dp)) {
                     Text("مقارنة المحافظات الموسمية", style = MaterialTheme.typography.titleMedium)
                     if (provinceSeasonalityRows.isEmpty()) {
-                        Text("لا توجد بيانات مقارنة بعد.")
+                        FushInlineState("لا توجد بيانات مقارنة بين المحافظات بعد.")
                     } else {
                         val names = policies.associate { it.code to it.nameAr }
                         Column(Modifier.fillMaxWidth().horizontalScroll(rememberScrollState())) {
@@ -629,7 +629,7 @@ fun PlanningScreen(
 
         val history = forecast?.history.orEmpty().reversed()
         if (history.isEmpty()) {
-            item { Text("لا توجد بيانات تاريخية متاحة لهذا الاختيار.") }
+            item { FushEmptyState("لا توجد بيانات تاريخية", "لا تتوفر حركات طلب سابقة كافية لهذا المنتج والمحافظة ضمن نافذة التحليل الحالية.") }
         } else {
             items(history) { row ->
                 Card(Modifier.fillMaxWidth()) {
