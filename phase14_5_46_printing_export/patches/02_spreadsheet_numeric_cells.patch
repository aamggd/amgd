diff --git a/app/src/main/java/com/fush/erp/ui/export/SpreadsheetCellValue.kt b/app/src/main/java/com/fush/erp/ui/export/SpreadsheetCellValue.kt
new file mode 100644
index 0000000..5c266e7
--- /dev/null
+++ b/app/src/main/java/com/fush/erp/ui/export/SpreadsheetCellValue.kt
@@ -0,0 +1,30 @@
+package com.fush.erp.ui.export
+
+enum class SpreadsheetCellKind { TEXT, NUMBER, CURRENCY }
+
+data class SpreadsheetCellValue(
+    val kind: SpreadsheetCellKind,
+    val number: Double? = null
+)
+
+private val currencyCellRegex = Regex("^([+-]?(?:\\d{1,3}(?:,\\d{3})*|\\d+)(?:\\.\\d+)?)\\s*ر\\.ي$")
+private val numberCellRegex = Regex("^[+-]?(?:\\d{1,3}(?:,\\d{3})*|\\d+)(?:\\.\\d+)?$")
+
+fun parseSpreadsheetCell(raw: String): SpreadsheetCellValue {
+    val value = raw.trim()
+    currencyCellRegex.matchEntire(value)?.let { match ->
+        val parsed = match.groupValues[1].replace(",", "").toDoubleOrNull()
+        if (parsed != null) return SpreadsheetCellValue(SpreadsheetCellKind.CURRENCY, parsed)
+    }
+    if (numberCellRegex.matches(value)) {
+        val unsigned = value.removePrefix("+").removePrefix("-").replace(",", "")
+        val integerPart = unsigned.substringBefore('.')
+        val preservesLeadingZero = integerPart.length > 1 && integerPart.startsWith('0')
+        if (!preservesLeadingZero) {
+            value.replace(",", "").toDoubleOrNull()?.let {
+                return SpreadsheetCellValue(SpreadsheetCellKind.NUMBER, it)
+            }
+        }
+    }
+    return SpreadsheetCellValue(SpreadsheetCellKind.TEXT)
+}
diff --git a/app/src/test/java/com/fush/erp/ui/export/SpreadsheetCellValueTest.kt b/app/src/test/java/com/fush/erp/ui/export/SpreadsheetCellValueTest.kt
new file mode 100644
index 0000000..716290d
--- /dev/null
+++ b/app/src/test/java/com/fush/erp/ui/export/SpreadsheetCellValueTest.kt
@@ -0,0 +1,31 @@
+package com.fush.erp.ui.export
+
+import org.junit.Assert.assertEquals
+import org.junit.Assert.assertNull
+import org.junit.Test
+
+class SpreadsheetCellValueTest {
+    @Test
+    fun parsesYemeniCurrencyAsNumericCell() {
+        val parsed = parseSpreadsheetCell("125,000.50 ر.ي")
+        assertEquals(SpreadsheetCellKind.CURRENCY, parsed.kind)
+        assertEquals(125000.50, parsed.number ?: 0.0, 0.0001)
+    }
+
+    @Test
+    fun parsesPlainQuantityAsNumber() {
+        val parsed = parseSpreadsheetCell("360")
+        assertEquals(SpreadsheetCellKind.NUMBER, parsed.kind)
+        assertEquals(360.0, parsed.number ?: 0.0, 0.0001)
+    }
+
+    @Test
+    fun preservesCodesAndBalanceLabelsAsText() {
+        val code = parseSpreadsheetCell("00125")
+        val balance = parseSpreadsheetCell("100,000.00 ر.ي مدين")
+        assertEquals(SpreadsheetCellKind.TEXT, code.kind)
+        assertNull(code.number)
+        assertEquals(SpreadsheetCellKind.TEXT, balance.kind)
+        assertNull(balance.number)
+    }
+}
