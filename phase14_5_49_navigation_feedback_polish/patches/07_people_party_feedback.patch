diff --git a/app/src/main/java/com/fush/erp/ui/screens/EmployeeScreens.kt b/app/src/main/java/com/fush/erp/ui/screens/EmployeeScreens.kt
index fd519e0..62e4fe6 100644
--- a/app/src/main/java/com/fush/erp/ui/screens/EmployeeScreens.kt
+++ b/app/src/main/java/com/fush/erp/ui/screens/EmployeeScreens.kt
@@ -122,7 +122,7 @@ fun EmployeesScreen(container: AppContainer, user: UserEntity, modifier: Modifie
                         enabled = employees.isNotEmpty() && orders.any { it.status !in setOf("CLOSED", "REJECTED", "CANCELLED") },
                         modifier = Modifier.fillMaxWidth()
                     ) { Text("تعيين موظف إنتاج / مشغل لأمر") }
-                    message?.let { Text(it, color = MaterialTheme.colorScheme.primary) }
+                    FushOperationMessage(message, onConsumed = { message = null })
                 }
             }
         }
@@ -344,7 +344,7 @@ private fun EmployeeProfileScreen(
                     OutlinedButton(onClick = { showEdit = true }, modifier = Modifier.weight(1f)) { Text("تعديل الملف") }
                     OutlinedButton(onClick = { tab = 3 }, modifier = Modifier.weight(1f)) { Text("عرض المستحقات") }
                 }
-                message?.let { Text(it, color = MaterialTheme.colorScheme.primary) }
+                FushOperationMessage(message, onConsumed = { message = null })
             }
         }
         }
diff --git a/app/src/main/java/com/fush/erp/ui/screens/PartyScreens.kt b/app/src/main/java/com/fush/erp/ui/screens/PartyScreens.kt
index 1af6f54..f856b86 100644
--- a/app/src/main/java/com/fush/erp/ui/screens/PartyScreens.kt
+++ b/app/src/main/java/com/fush/erp/ui/screens/PartyScreens.kt
@@ -74,16 +74,8 @@ fun CustomersScreen(container: AppContainer, user: UserEntity, modifier: Modifie
             }
         }
 
-        message?.let { text ->
-            item {
-                Surface(
-                    color = MaterialTheme.colorScheme.secondaryContainer,
-                    contentColor = MaterialTheme.colorScheme.onSecondaryContainer,
-                    shape = MaterialTheme.shapes.medium,
-                ) {
-                    Text(text, Modifier.fillMaxWidth().padding(12.dp), style = MaterialTheme.typography.bodyMedium)
-                }
-            }
+        if (message != null) {
+            item { FushOperationMessage(message, onConsumed = { message = null }) }
         }
 
         item {
@@ -279,16 +271,8 @@ fun SuppliersScreen(container: AppContainer, user: UserEntity, modifier: Modifie
             }
         }
 
-        message?.let { text ->
-            item {
-                Surface(
-                    color = MaterialTheme.colorScheme.secondaryContainer,
-                    contentColor = MaterialTheme.colorScheme.onSecondaryContainer,
-                    shape = MaterialTheme.shapes.medium,
-                ) {
-                    Text(text, Modifier.fillMaxWidth().padding(12.dp), style = MaterialTheme.typography.bodyMedium)
-                }
-            }
+        if (message != null) {
+            item { FushOperationMessage(message, onConsumed = { message = null }) }
         }
 
         item {
@@ -510,9 +494,7 @@ private fun CustomerProfileScreen(container: AppContainer, user: UserEntity, cus
                     Button(onClick = { voucherType = "RECEIPT" }, modifier = Modifier.weight(1f), shape = MaterialTheme.shapes.medium) { Text("سند قبض") }
                     OutlinedButton(onClick = { voucherType = "PAYMENT" }, modifier = Modifier.weight(1f), shape = MaterialTheme.shapes.medium) { Text("سند صرف") }
                 }
-                message?.let { text ->
-                    FushNotice(text, tone = if (text.startsWith("تعذر")) FushStatusTone.Danger else FushStatusTone.Success)
-                }
+                FushOperationMessage(message, onConsumed = { message = null })
             }
         }
         item {
@@ -615,9 +597,7 @@ private fun SupplierProfileScreen(container: AppContainer, user: UserEntity, sup
                     Button(onClick = { voucherType = "PAYMENT" }, modifier = Modifier.weight(1f), shape = MaterialTheme.shapes.medium) { Text("سند صرف") }
                     OutlinedButton(onClick = { voucherType = "RECEIPT" }, modifier = Modifier.weight(1f), shape = MaterialTheme.shapes.medium) { Text("سند قبض") }
                 }
-                message?.let { text ->
-                    FushNotice(text, tone = if (text.startsWith("تعذر")) FushStatusTone.Danger else FushStatusTone.Success)
-                }
+                FushOperationMessage(message, onConsumed = { message = null })
             }
         }
         item {
diff --git a/app/src/main/java/com/fush/erp/ui/screens/SalesRepresentativeScreens.kt b/app/src/main/java/com/fush/erp/ui/screens/SalesRepresentativeScreens.kt
index df29e9a..54c8e51 100644
--- a/app/src/main/java/com/fush/erp/ui/screens/SalesRepresentativeScreens.kt
+++ b/app/src/main/java/com/fush/erp/ui/screens/SalesRepresentativeScreens.kt
@@ -77,7 +77,7 @@ fun SalesRepresentativesScreen(container: AppContainer, user: UserEntity, modifi
         }
         item {
             Button(onClick = { showAdd = true }, modifier = Modifier.fillMaxWidth()) { Text("إضافة مندوب مبيعات") }
-            message?.let { Text(it, color = MaterialTheme.colorScheme.primary, modifier = Modifier.padding(top = 6.dp)) }
+            FushOperationMessage(message, onConsumed = { message = null })
         }
         item {
             OutlinedTextField(
@@ -188,7 +188,7 @@ private fun SalesRepresentativeProfile(
                     OutlinedButton(onClick = { showAssignCustomer = true }, enabled = rep.status == "ACTIVE", modifier = Modifier.weight(1f)) { Text("ربط عميل") }
                     Button(onClick = { showPayment = true }, enabled = rep.status == "ACTIVE" && due > 0.000001, modifier = Modifier.weight(1f)) { Text("صرف عمولة") }
                 }
-                message?.let { Text(it, color = MaterialTheme.colorScheme.primary) }
+                FushOperationMessage(message, onConsumed = { message = null })
             }
         }
         }
