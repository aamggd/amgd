name: Audit FUSH Final Integration 14.5.48 V2

on:
  push:
    branches: [fush/integration-final-14.5.48]
    paths:
      - '.github/workflows/integration-14.5.48-conflict-audit-v2.yml'
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  conflict-audit-v2:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Download official baseline 14.5.38
        uses: actions/download-artifact@v4
        with:
          name: FushERP-Phase14.5.38-Professional-UI-Build
          path: artifacts/baseline
          github-token: ${{ github.token }}
          repository: aamggd/amgd
          run-id: 31858347166
      - name: Download verified UI 14.5.43
        uses: actions/download-artifact@v4
        with:
          name: FushERP-Phase14.5.43-Integrated-Professional-UI-Build
          path: artifacts/ui43
          github-token: ${{ github.token }}
          repository: aamggd/amgd
          run-id: 31861273525
      - name: Download validated Accounting 14.5.39
        uses: actions/download-artifact@v4
        with:
          name: FushERP-Accounting-Phase14.5.39-FX-Treasury-Validated-Build
          path: artifacts/accounting
          github-token: ${{ github.token }}
          repository: aamggd/amgd
          run-id: 31865418288
      - name: Download validated Security final
        uses: actions/download-artifact@v4
        with:
          name: FushERP-Security-Rebase-Final-Validation
          path: artifacts/security
          github-token: ${{ github.token }}
          repository: aamggd/amgd
          run-id: 31865181875
      - name: Download validated Reports and Printing 14.5.46
        uses: actions/download-artifact@v4
        with:
          name: FushERP-Phase14.5.46-Professional-Printing-Build
          path: artifacts/reports
          github-token: ${{ github.token }}
          repository: aamggd/amgd
          run-id: 31865897444
      - name: Reconstruct latest UI from verified UI43
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p work/{baseline,ui,accounting,security,reports}
          unzip -q artifacts/baseline/FushERP-Mobile-Phase14.5.38-Professional-UI-Source.zip -d work/baseline
          unzip -q artifacts/ui43/FushERP-Mobile-Phase14.5.43-Integrated-Professional-UI-Source.zip -d work/ui
          unzip -q artifacts/accounting/FushERP-Mobile-Phase14.5.39-Foreign-Currency-Treasury-Revaluation-Source.zip -d work/accounting
          unzip -q artifacts/security/FushERP-Security-Rebase-Final-Source.zip -d work/security
          unzip -q artifacts/reports/FushERP-Mobile-Phase14.5.46-Professional-Printing-Source.zip -d work/reports
          UI=$(find work/ui -mindepth 1 -maxdepth 1 -type d | head -1)
          rm -rf "$UI/.git" "$UI/app/build" "$UI/.gradle" "$UI/.kotlin" "$UI/build"
          git -C "$UI" init -q
          git -C "$UI" config user.email integration@fush.local
          git -C "$UI" config user.name 'FUSH Integration'
          git -C "$UI" add -A
          git -C "$UI" commit -qm ui43

          HOME_SHELL='app/src/main/java/com/fush/erp/ui/screens/HomeShell.kt'
          for n in 01_scope 02_handoff_manifest 03_build_gradle 04_shared_inline_states 05_governance_risk 06_planning_geography 07_operations_state_consistency 08_people_profile_states 09_production_purchase_states 10_sales_rep_sales_states; do
            p="$GITHUB_WORKSPACE/phase14_5_44_final_ui_handoff/patches/${n}.patch"
            echo "Applying UI44 $n"
            if [ "$n" = '07_operations_state_consistency' ]; then
              git -C "$UI" apply --check --exclude="$HOME_SHELL" "$p"
              git -C "$UI" apply --exclude="$HOME_SHELL" "$p"
              (cd "$UI" && python3 "$GITHUB_WORKSPACE/integration_ui_14_5_44/resolve_home_shell_07.py")
            else
              git -C "$UI" apply --check "$p"
              git -C "$UI" apply "$p"
            fi
          done
          for d in phase14_5_45_inventory_scroll_hotfix phase14_5_46_scroll_profile_polish phase14_5_47_professional_forms_ui; do
            echo "Applying $d"
            while IFS= read -r p; do
              echo "  -> $p"
              git -C "$UI" apply --check "$p"
              git -C "$UI" apply "$p"
            done < <(find "$GITHUB_WORKSPACE/$d/patches" -maxdepth 1 -type f -name '*.patch' | sort -V)
          done
          git -C "$UI" diff --check
          grep -q 'applicationId = "com.fush.erp.recovery"' "$UI/app/build.gradle.kts"
          grep -q 'versionCode = 86' "$UI/app/build.gradle.kts"
          grep -q 'FUSH_DB_SCHEMA_VERSION = 27' "$UI/app/src/main/java/com/fush/erp/data/FushDatabase.kt"
          rm -rf "$UI/.git"
      - name: Audit three-way conflicts
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import pathlib, subprocess, hashlib
          root=pathlib.Path('work')
          def one(path):
              ds=[p for p in path.iterdir() if p.is_dir()]
              assert len(ds)==1,(path,ds)
              return ds[0]
          base=one(root/'baseline'); ui=one(root/'ui')
          specs={'ACCOUNTING':one(root/'accounting'),'SECURITY':one(root/'security'),'REPORTS_PRINTING':one(root/'reports')}
          def files(d):
              out=set()
              for p in d.rglob('*'):
                  if not p.is_file(): continue
                  s=str(p)
                  if '/build/' in s or '/.gradle/' in s or '/.kotlin/' in s or s.endswith('.apk'): continue
                  out.add(str(p.relative_to(d)))
              return out
          def same(a,b):
              return a.exists() and b.exists() and hashlib.sha256(a.read_bytes()).digest()==hashlib.sha256(b.read_bytes()).digest()
          report=[]
          for label,spec in specs.items():
              changed=[]; conflicts=[]; new=[]; deleted=[]
              for rel in sorted(files(base)|files(spec)):
                  b=base/rel; t=spec/rel
                  if b.exists() and t.exists() and same(b,t): continue
                  changed.append(rel)
                  if not b.exists() and t.exists(): new.append(rel); continue
                  if b.exists() and not t.exists(): deleted.append(rel); continue
                  o=ui/rel
                  if not o.exists(): continue
                  ob=o.read_bytes(); bb=b.read_bytes(); tb=t.read_bytes()
                  if b'\0' in ob+bb+tb: continue
                  cp=subprocess.run(['git','merge-file','-p',str(o),str(b),str(t)],stdout=subprocess.PIPE,stderr=subprocess.PIPE)
                  if cp.returncode==1: conflicts.append(rel)
                  elif cp.returncode>1: conflicts.append(rel+' [merge-error]')
              report += [f'## {label}',f'changed={len(changed)} new={len(new)} deleted={len(deleted)} conflicts={len(conflicts)}']
              report += [f'CONFLICT {x}' for x in conflicts]
              report.append('')
          pathlib.Path('integration-conflict-audit.txt').write_text('\n'.join(report),encoding='utf-8')
          print('\n'.join(report))
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: FushERP-14.5.48-Conflict-Audit-V2
          path: integration-conflict-audit.txt
          retention-days: 7
