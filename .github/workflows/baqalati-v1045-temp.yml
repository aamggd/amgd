name: Baqalati v1.0.46 Temp Build

on:
  push:
    branches: [ci-baqalati-v1045-20260916]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - name: Retrieve temporary build input
        shell: bash
        run: |
          set -euo pipefail
          base='https://api.firestorage.ai/prod/file'
          share='5rVHrPbZvl-g'
          file_id='01a0aaf78f0675feaed8593acbd8bad4'
          python3 - <<'PY'
          import json, urllib.request
          base='https://api.firestorage.ai/prod/file'
          share='5rVHrPbZvl-g'
          file_id='01a0aaf78f0675feaed8593acbd8bad4'
          req=urllib.request.Request(f'{base}/shares/{share}/files/{file_id}/download',method='POST')
          with urllib.request.urlopen(req, timeout=30) as r:
              meta=json.load(r)
          url=meta['downloadUrl']
          with urllib.request.urlopen(url, timeout=60) as r, open('/tmp/source.zip','wb') as out:
              out.write(r.read())
          PY
          unzip -t /tmp/source.zip
          mkdir source
          unzip -q /tmp/source.zip -d source
          mkdir -p source/docs/ui
          test -f source/settings.gradle.kts
      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
      - name: Install Android SDK packages
        shell: bash
        run: |
          set -euo pipefail
          export ANDROID_HOME=/usr/local/lib/android/sdk
          export ANDROID_SDK_ROOT="$ANDROID_HOME"
          export PATH="$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools:$PATH"
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager 'platform-tools' 'platforms;android-36' 'build-tools;36.0.0'
      - name: Set up Gradle 9.4.1
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.4.1'
      - name: Verify source contracts
        working-directory: source
        run: python3 scripts/verify_full_ui_source_gate.py
      - name: Run unit tests
        working-directory: source
        run: gradle --no-daemon :app:testDebugUnitTest
      - name: Build unsigned release APK
        working-directory: source
        run: gradle --no-daemon :app:assembleRelease
      - name: Stage APK and signing tools
        shell: bash
        run: |
          set -euo pipefail
          export ANDROID_HOME=/usr/local/lib/android/sdk
          mkdir -p out/build-tools/lib
          cp source/app/build/outputs/apk/release/app-release-unsigned.apk out/Baqalati-v1.0.46-UNSIGNED.apk
          cp "$ANDROID_HOME/build-tools/36.0.0/lib/apksigner.jar" out/build-tools/lib/apksigner.jar
          cp "$ANDROID_HOME/build-tools/36.0.0/zipalign" out/build-tools/zipalign
          cp "$ANDROID_HOME/build-tools/36.0.0/aapt2" out/build-tools/aapt2
          chmod +x out/build-tools/zipalign out/build-tools/aapt2
          sha256sum out/Baqalati-v1.0.46-UNSIGNED.apk > out/Baqalati-v1.0.46-UNSIGNED.apk.sha256
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Baqalati-v1.0.46-build
          path: out
          if-no-files-found: error
