name: Baqalati Firestorage Resolve V6
on:
  push:
    branches: [baqalati-secure-build-r3]
    paths: ['.github/workflows/baqalati-firestorage-resolve-v6.yml']
permissions:
  contents: read
jobs:
  resolve:
    runs-on: ubuntu-latest
    steps:
      - name: Resolve signed download URL
        shell: bash
        run: |
          set -euo pipefail
          API='https://api.firestorage.ai/dev/file'
          SHARE='ub19BE9VJ3mj'
          curl -fsSL "$API/shares/$SHARE/files?maxResults=1000" -o files.json
          FILE_ID=$(python3 -c 'import json; d=json.load(open("files.json")); print(d["files"][0]["fileId"])')
          curl -fsSL -X POST "$API/shares/$SHARE/files/$FILE_ID/download" -o download.json
          python3 -c 'import json; print(json.load(open("download.json"))["downloadUrl"])' > download-url-v6.txt
      - uses: actions/upload-artifact@v4
        with:
          name: encrypted-payload-download-url-v6
          path: download-url-v6.txt
          retention-days: 1
# trigger: resolve current V6 payload
