name: FUSH Inventory P1 Legacy Validation (superseded)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - name: Superseded workflow notice
        run: |
          echo 'This workflow is retained only for history.'
          echo 'Authoritative Inventory P1 validation: .github/workflows/fush-inventory-p1-candidate.yml'
          echo 'Validated run: 31983051143'
