name: FUSH Customer v216 Firestorage Endpoint Probe

on:
  push:
    branches: [fush-customer-v216-build]

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Inspect firestorage client endpoint
        run: bash ci/v216_probe_script.sh
