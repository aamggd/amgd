name: FUSH Customer v217 Commercial Release Gate

on:
  push:
    branches: [fush-customer-v217-build]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 75
    steps:
      - uses: actions/checkout@v4

      - name: Download and verify exact v217 source
        shell: bash
        run: |
          set -euo pipefail
          BASE='https://api.firestorage.ai/dev/file'
          SHARE='hSB6vBeb5MzG'
          FILE='01a088750f917532a9598861091df813'
          COOKIE=/tmp/firestorage.cookies
          rm -f "$COOKIE"
          curl -fsS -c "$COOKIE" -b "$COOKIE" \
            -H 'Content-Type: application/json' \
            -X POST "$BASE/shares/$SHARE/cushion-sessions" \
            --data '{"password":null}' \
            -o /tmp/cushion.json
          curl -fsS -c "$COOKIE" -b "$COOKIE" \
            -X POST "$BASE/shares/$SHARE/files/$FILE/download" \
            -o /tmp/download.json
          DOWNLOAD_URL="$(python3 - <<'PY'
          import json
          with open('/tmp/download.json', encoding='utf-8') as f:
              data = json.load(f)
          url = data.get('downloadUrl')
          if not url:
              raise SystemExit('downloadUrl missing')
          print(url)
          PY
          )"
          curl -fL "$DOWNLOAD_URL" -o /tmp/v217-source.zip
          echo '02d50467014f0bc21243ba676d6bd0848390cd6f4e5f1d708535171e0731f680  /tmp/v217-source.zip' | sha256sum -c -
          rm -rf v217-src
          mkdir -p v217-src
          unzip -oq /tmp/v217-source.zip -d v217-src
          test -f v217-src/settings.gradle.kts
          test -f v217-src/app/build.gradle.kts

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - uses: android-actions/setup-android@v3

      - name: Install Android SDK 36
        shell: bash
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager 'platforms;android-36' 'build-tools;36.1.0'

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.4.1'

      - name: Verify v217 commercial release contract
        shell: bash
        run: |
          cd v217-src
          python3 tools/verify_v217_commercial_release_gate.py

      - name: Unit tests
        shell: bash
        run: |
          cd v217-src
          gradle --no-daemon :app:testDebugUnitTest

      - name: Assemble release APK
        shell: bash
        run: |
          cd v217-src
          gradle --no-daemon :app:assembleRelease

      - name: Build release AAB
        shell: bash
        run: |
          cd v217-src
          gradle --no-daemon :app:bundleRelease

      - name: Release lint
        shell: bash
        run: |
          cd v217-src
          gradle --no-daemon :app:lintVitalRelease

      - name: Align and identify release outputs
        shell: bash
        run: |
          set -euo pipefail
          cd v217-src
          ZIPALIGN="$ANDROID_HOME/build-tools/36.1.0/zipalign"
          AAPT2="$ANDROID_HOME/build-tools/36.1.0/aapt2"
          APK='app/build/outputs/apk/release/app-release-unsigned.apk'
          AAB='app/build/outputs/bundle/release/app-release.aab'
          test -f "$APK"
          test -f "$AAB"
          APK_OUT='FUSH-Customer-v217-CommercialReleaseGate-UNSIGNED-ALIGNED.apk'
          AAB_OUT='FUSH-Customer-v217-CommercialReleaseGate-UNSIGNED.aab'
          "$ZIPALIGN" -f -v 4 "$APK" "$APK_OUT"
          "$ZIPALIGN" -c -v 4 "$APK_OUT"
          cp "$AAB" "$AAB_OUT"
          "$AAPT2" dump badging "$APK_OUT" | head -20 | tee APK_IDENTITY.txt
          unzip -t "$AAB_OUT" >/dev/null
          sha256sum "$APK_OUT" "$AAB_OUT" > SHA256SUMS.txt

      - uses: actions/upload-artifact@v4
        with:
          name: FUSH-Customer-v217-Commercial-Release-Unsigned
          path: |
            v217-src/FUSH-Customer-v217-CommercialReleaseGate-UNSIGNED-ALIGNED.apk
            v217-src/FUSH-Customer-v217-CommercialReleaseGate-UNSIGNED.aab
            v217-src/APK_IDENTITY.txt
            v217-src/SHA256SUMS.txt
          if-no-files-found: error
