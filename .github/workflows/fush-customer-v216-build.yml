name: FUSH Customer v218 Room54 Integrity Hotfix

on:
  push:
    branches: [fush-customer-v218-room54-integrity-hotfix]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      - name: Download and verify exact v218 source
        shell: bash
        run: |
          set -euo pipefail
          BASE='https://api.firestorage.ai/dev/file'
          SHARE='Jbvv6MFE7iAr'
          FILE='01a0889e333a7276b96a227789531fc2'
          COOKIE=/tmp/firestorage.cookies
          rm -f "$COOKIE"
          curl -fsS -c "$COOKIE" -b "$COOKIE" \
            -H 'Content-Type: application/json' \
            -X POST "$BASE/shares/$SHARE/cushion-sessions" \
            --data '{"password":null}' \
            -o /tmp/cushion.json
          curl -fsS -c "$COOKIE" -b "$COOKIE" \
            -X POST "$BASE/shares/$SHARE/files/$FILE/download" \
            -o /tmp/download.json
          DOWNLOAD_URL="$(python3 - <<'PY'
          import json
          with open('/tmp/download.json', encoding='utf-8') as f:
              data = json.load(f)
          url = data.get('downloadUrl')
          if not url:
              raise SystemExit('downloadUrl missing')
          print(url)
          PY
          )"
          curl -fL "$DOWNLOAD_URL" -o /tmp/v218-source.zip
          echo 'ecfe372cfc2c298a8ed1ecc790b98da80bbc1291ed8bbade5ae561a672a2c03b  /tmp/v218-source.zip' | sha256sum -c -
          rm -rf v218-src
          mkdir -p v218-src
          unzip -oq /tmp/v218-source.zip -d v218-src
          test -f v218-src/settings.gradle.kts
          test -f v218-src/app/build.gradle.kts

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - uses: android-actions/setup-android@v3

      - name: Install Android SDK 36
        shell: bash
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager 'platforms;android-36' 'build-tools;36.1.0'

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.4.1'

      - name: Verify v218 Room integrity contract
        shell: bash
        run: |
          cd v218-src
          python3 tools/verify_v218_room54_integrity_hotfix.py

      - name: Unit tests
        shell: bash
        run: |
          cd v218-src
          gradle --no-daemon :app:testDebugUnitTest

      - name: Compile Android migration tests
        shell: bash
        run: |
          cd v218-src
          gradle --no-daemon :app:assembleDebugAndroidTest

      - name: Run Room 54 to 55 migration tests on emulator
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 35
          arch: x86_64
          target: default
          disable-animations: true
          script: cd v218-src && gradle --no-daemon connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.fush.erp.data.Room54IntegrityHotfixMigrationTest

      - name: Assemble release APK
        shell: bash
        run: |
          cd v218-src
          gradle --no-daemon :app:assembleRelease

      - name: Build release AAB
        shell: bash
        run: |
          cd v218-src
          gradle --no-daemon :app:bundleRelease

      - name: Release lint
        shell: bash
        run: |
          cd v218-src
          gradle --no-daemon :app:lintVitalRelease

      - name: Align and identify release outputs
        shell: bash
        run: |
          set -euo pipefail
          cd v218-src
          ZIPALIGN="$ANDROID_HOME/build-tools/36.1.0/zipalign"
          AAPT2="$ANDROID_HOME/build-tools/36.1.0/aapt2"
          APK='app/build/outputs/apk/release/app-release-unsigned.apk'
          AAB='app/build/outputs/bundle/release/app-release.aab'
          test -f "$APK"
          test -f "$AAB"
          APK_OUT='FUSH-Customer-v218-Room54IntegrityHotfix-UNSIGNED-ALIGNED.apk'
          AAB_OUT='FUSH-Customer-v218-Room54IntegrityHotfix-UNSIGNED.aab'
          "$ZIPALIGN" -f -v 4 "$APK" "$APK_OUT"
          "$ZIPALIGN" -c -v 4 "$APK_OUT"
          cp "$AAB" "$AAB_OUT"
          "$AAPT2" dump badging "$APK_OUT" | head -20 | tee APK_IDENTITY.txt
          unzip -t "$AAB_OUT" >/dev/null
          sha256sum "$APK_OUT" "$AAB_OUT" > SHA256SUMS.txt

      - uses: actions/upload-artifact@v4
        with:
          name: FUSH-Customer-v218-Room54IntegrityHotfix-Unsigned
          path: |
            v218-src/FUSH-Customer-v218-Room54IntegrityHotfix-UNSIGNED-ALIGNED.apk
            v218-src/FUSH-Customer-v218-Room54IntegrityHotfix-UNSIGNED.aab
            v218-src/APK_IDENTITY.txt
            v218-src/SHA256SUMS.txt
          if-no-files-found: error
